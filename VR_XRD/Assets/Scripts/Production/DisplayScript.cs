using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DisplayScript : MonoBehaviour
{

    //Script skal bruges til forkslellige dele af et display af form ligsom eksempel vi så
    //den skal basically bare have nogle refferancer og get sets tænker jeg
    //den rigtige opgave er at lave det i scenen idk how yet.. Tænker det skal være i space, ikke bare hud


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
